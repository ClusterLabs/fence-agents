name = "fence_ipmilan"
agent_type = "binmitm"
agent = "fence_ipmilan"

[options] # options for fence-agent
    # option = [ "value", "long-opt", "short-opt" ]
        # option: name of the option which is used in fence-agent stdin
        # short-opt: not required
	login = [ "user", "--username", "-l" ]
	passwd = [ "pass", "--password", "-p" ]
	ipaddr = [ "fence_ipmilan.test", "--ip", "-a" ]
	lanplus = [ None, "--lanplus", "-P"]
	ipmitool_path = [ "./binmitm.py /usr/bin/ipmitool", "--ipmitool-path" ]
